package com.isst.demo.service;

import org.springframework.stereotype.Service;

import com.isst.demo.dto.ReservaDTO;

@Service
public class ReservaService {

    // Método para procesar y guardar la reserva en la base de datos
    public void crearReserva(ReservaDTO reservaDTO) {
        // Aquí iría la lógica para guardar la reserva en la base de datos
        // Por ejemplo, llamar a un repositorio de Spring Data JPA para guardar la reserva en la base de datos
    }
}
